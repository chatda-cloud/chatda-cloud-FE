{
  "buildCommand": "git clone https://github.com/flutter/flutter.git --branch stable --depth 1 && flutter/bin/flutter build web --release",
  "outputDirectory": "build/web",
  "installCommand": "echo skip"
}
